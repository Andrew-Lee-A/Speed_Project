import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#0e111d',
    },

    secondary: {
      main: '#1D2133',
      secondary: '#212124',
    },
  },
})

export default theme
